{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Make NSIS Installer",
            "type": "shell",
            "command": "makensis -INPUTCHARSET UTF8 ./windows.nsi && wine ./windows.exe"
        }
    ]
}